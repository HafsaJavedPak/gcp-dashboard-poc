# Copy to .env (same folder) and change the value. .env is gitignored.
# This is the password for Metabase's internal Postgres metadata DB,
# NOT anything to do with BigQuery.
MB_DB_PASS=change-me-to-a-long-random-string
